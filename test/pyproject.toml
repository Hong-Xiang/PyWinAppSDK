[project]
name = "test"
version = "0.1.0"
description = "Integration tests for PyWinAppSDK"
readme = "README.md"
requires-python = ">=3.11, <3.14"
dependencies = [
    "winappsdk-headers",
    "winappsdk-foundation",
    "winappsdk-ai",
    "winappsdk-interactiveexperiences",
]

[tool.uv]
find-links = ["../wheels"]
